# Lab_Demo (Code from PDF)

This folder contains the **demo code that appears in the PDF**: a client-server booking system using threads + blocking queues (no networking).

## Compile & Run

From `Lab_Demo`:

```bash
javac reservation/*.java
java reservation.ReservationsMain
```
